{
  "name": "itpas",
  "version": "1.0.0",
  "description": "ITPAS Glitch Configuration",
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
